#include <stdio.h>

/**
 * main - program that prints all single digit numbers of base 10 starting from
 * All your code should be in the main function
 * Return: 0
 */

int main(void)
{
int number;
for (number = 48; number <= 57; number++)kk
{
putchar(number);
}
putchar('\n');
return (0);
}
