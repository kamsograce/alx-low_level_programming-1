#include "main."

/**
 * _isdigit - check if character is a digit
 * (0 through 9)
 *
 * @c: character to check.
 * Return: returns 1 if True
 * 0 if otherwise
 */

int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
