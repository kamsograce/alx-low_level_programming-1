#include "main.h"

/**
 * _isdigit - type int.
 * @c: character to check.
 *
 * Description: checks for a digit 0-9
 * Return: retrns 1 or 0
 */

int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
