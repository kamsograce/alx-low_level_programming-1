#include "main.h"

/**
 * _isupper - checks if character is uppercase
 * @c: character to check.
 *
 * Return: 1 if uppercase
 * 0 if lowercase
 */

int _isupper(int c)
{
	return (c >= 65 && c <= 90);
}
